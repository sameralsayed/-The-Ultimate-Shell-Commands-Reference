# 🐚 The Ultimate Shell Commands Reference

A curated collection of essential shell commands for Linux, macOS, and other Unix-like systems. Perfect for beginners and seasoned developers who need a quick, practical lookup.

---

## 🏷️ Tags
`#shell` `#bash` `#linux` `#commands` `#terminal` `#productivity`

## ✨ Features
- 📂 **Categorized** – find what you need fast  
- 🔍 **Quick lookup** – each command includes a short description  
- 🎨 **Emoji indicators** – visual cues for every line  
- 💡 **Practical examples** – see real‑world usage

---

## 📁 File & Directory Operations
- 📂 `ls` – list directory contents  
- 📁 `cd` – change directory  
- 🏠 `pwd` – print working directory  
- 📄 `touch` – create an empty file or update timestamp  
- 📝 `cat` – concatenate and display files  
- ✂️ `cp` – copy files or directories  
- 🚚 `mv` – move or rename files/directories  
- 🗑️ `rm` – remove files or directories (use with caution)  
- 🗂️ `mkdir` – create a new directory  
- 🗑️ `rmdir` – remove an empty directory  
- 🔗 `ln` – create hard or symbolic links  

## 🔍 Viewing & Searching
- 📖 `less` – view file content page by page  
- 📜 `head` – output the first part of a file  
- 📃 `tail` – output the last part of a file (use `-f` to follow)  
- 🔎 `grep` – search text using patterns  
- 🔎 `find` – search for files and directories  
- 📊 `wc` – count lines, words, and characters  
- 🧩 `locate` – quickly find files by name (uses a database)  

## 🔐 Permissions & Ownership
- 🔒 `chmod` – change file permissions  
- 👤 `chown` – change file owner and group  
- 👥 `chgrp` – change group ownership  
- 🛡️ `umask` – set default permission mask  

## ⚙️ Process Management
- 📋 `ps` – display running processes  
- 🧵 `top` – interactive process viewer  
- 🔄 `htop` – improved interactive process viewer (if installed)  
- ⚡ `kill` – send a signal to a process (e.g., terminate)  
- 🛑 `pkill` – kill processes by name  
- 🚦 `jobs` – list background jobs  
- 🔛 `bg` – resume a job in the background  
- 🔛 `fg` – resume a job in the foreground  

## 🌐 Networking
- 📡 `ping` – test network connectivity  
- 🌍 `curl` – transfer data from or to a server  
- 📥 `wget` – download files from the web  
- 🔌 `netstat` – display network connections and statistics  
- 🔎 `ss` – modern replacement for netstat  
- 🧭 `ip` – show/manage network interfaces and routing  
- 🚪 `ssh` – secure shell remote login  
- 📁 `scp` – secure copy over SSH  

## 📦 Archiving & Compression
- 🗜️ `tar` – archive files (often with compression)  
  - `tar -czvf archive.tar.gz /path` – create a gzipped tarball  
  - `tar -xzvf archive.tar.gz` – extract a gzipped tarball  
- 💨 `gzip` / `gunzip` – compress/decompress files  
- 📦 `zip` / `unzip` – manage zip archives  

## 🛠️ System Information
- 🖥️ `uname` – display system information  
- 📀 `df` – show disk space usage  
- 📊 `du` – estimate file/directory space usage  
- 💾 `free` – display memory usage  
- ⏰ `uptime` – show how long the system has been running  
- 🔌 `dmesg` – print kernel messages  

## ✍️ Text Manipulation
- ✂️ `cut` – extract sections from lines  
- 🔄 `sort` – sort lines of text  
- 🔁 `uniq` – report or omit repeated lines  
- 🔍 `sed` – stream editor for filtering and transforming text  
- 📊 `awk` – powerful text processing language  
- 🔡 `tr` – translate or delete characters  
- 🔗 `paste` – merge lines of files  

## 🔄 Aliases & Shortcuts
- 📝 `alias` – create a shortcut for a command  
  - `alias ll='ls -alF'` – example alias  
- ❌ `unalias` – remove an alias  
- ⌨️ `Ctrl + R` – reverse search in history  
- 📜 `history` – show command history  

## 🧹 Cleanup & Utilities
- 🧽 `clear` – clear the terminal screen  
- 🔄 `echo` – print text to standard output  
- 🕒 `date` – display or set system date/time  
- 🔢 `bc` – arbitrary precision calculator  
- 🧩 `xargs` – build and execute command lines from input  

---

## 📌 Quick Tip
Most commands support `--help` or `man <command>` to display detailed documentation.

---

Made with ♥️ by [SAMER SAEID](https://bio.link/samer)
